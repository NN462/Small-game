# Cloud Cat multiplayer WebSocket server (Render / Railway / any Node host)
services:
  - type: web
    name: cloud-cat-ws
    runtime: node
    rootDir: ./server
    buildCommand: npm install
    startCommand: npm start
    plan: free
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8787
